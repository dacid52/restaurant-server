spring:
  datasource:
    url: jdbc:mysql://localhost:3306/userdb?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=UTF-8
    username: root
    password: ""
  # -------------------------------------------------------
  # Cấu hình mail cho local dev — Gmail SMTP + App Password
  # Tạo App Password: https://myaccount.google.com/apppasswords
  # -------------------------------------------------------
  mail:
    host: smtp.gmail.com
    port: 587
    username: "huy211439@gmail.com"  # Thay bằng email Gmail của bạn
    password: "fbikwnsabgfjgorq"
    properties:
      mail:
        smtp:
          auth: true
          from: "huy211439@gmail.com"
          auth.mechanisms: LOGIN PLAIN
          starttls:
            enable: true
            required: true

app:
  base-url: http://localhost:3000
  frontend-url: http://localhost:3001
  mail:
    fail-on-error: false
